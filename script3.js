const concatnate = (arg1, arg2) => {
  const result = arg1.toString() + arg2.toString();
  alert("The concatnated string is🍋: " + result);
  return result;
};

concatnate(true, 8);
// concatnate(2, "hello");
